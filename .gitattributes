# macOS bash-скрипты ДОЛЖНЫ быть с LF (CRLF ломает shebang).
*.sh text eol=lf
# Windows PowerShell — CRLF (BOM сохраняется git независимо от eol).
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

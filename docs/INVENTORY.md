# Реестр фич — Hamidun Setup

**Дата:** 2026-08-25
**Как собрано:** инвентаризация в три независимых прохода; каждая находка отдельно проверялась на опровержение (пыталась быть опровергнута по исходникам, собранным артефактам и прогонам), в реестр попало только то, что попытку пережило.

**Всего фич: 61. Покрыто тестами: 60 (98%).**
**Не покрыто 1.** Явное «нет» — 1. Теста не нашлось — 0.

Единственная непокрытая — маяк завершения курса, и это решение, а не пробел:
подписывать его нечем, пока владелец не заведёт секрет.

**Как эти числа менялись.** Сначала здесь стояло «покрыто 47, не покрыто 14», и это
было неправдой в сторону пессимизма: восемь фич тесты имели, инвентаризация их просто
не нашла — обнаружили агенты, писавшие спеки, потому что искали целенаправленно по
коду (29.08). Оставшиеся пять закрыты 30.08 настоящими тестами: renderer гоняется
целиком в `test/dom-harness.js`, а две записи на диск вынесены в модули
(`src/nomad-key.js`, `src/credentials-merge.js`) — из `main.js` их было не позвать,
потому что он требует `electron`, и ровно поэтому у них годами не было проверок.

Занижение покрытия — не безобидная ошибка: по нему решают, что писать дальше, и
завышенный долг уводит силы туда, где всё уже сделано. Поэтому числа в шапках
сверяются машиной со строками этой же таблицы (`npm run specs:check`, проверено
семью подлогами), а каждая цитата теста — на ДОСЛОВНОЕ присутствие в `test/`.

Прогон на чистом чекауте (CI, коммит 725c954): Windows — **438 прошло, 0 упало,
2 пропущено**; macOS — **394 прошло, 0 упало, 10 пропущено** (пропуски честные:
не та ОС).

Колонка «Спека» заполнена: 61 документ в [`docs/specs/`](specs/README.md), каждый
проверяется гейтом на существование путей и дословность заголовков тестов.

---

## Установка компонентов (22; тестами покрыто 22)

Что человек выбирает галочками и что происходит после нажатия «Установить». В `components.json` — 8 групп, 17 записей, из них 16 показываются пользователю (`verify` помечен `hidden: true`).

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Git | Ставит систему контроля версий, чтобы Claude сам сохранял историю проекта и откатывал правки. Три источника по очереди: вшитый exe, winget, прямая загрузка. | `components.json:8-20`, `scripts/windows/git.ps1:64-92`, `scripts/macos/git.sh` | есть | нет |
| Node.js LTS | Движок, на котором работает сам Claude Code и всё, что человек соберёт на JS. На macOS `.pkg` ставится через root-staging с пином Team ID. | `components.json:21-34`, `scripts/windows/node.ps1:107-134`, `scripts/macos/node.sh` | есть | нет |
| VS Code + два расширения | Редактор и панель Claude внутри него, чтобы новичок не сидел в терминале. Успех засчитывается только при обоих расширениях. | `components.json:36-49`, `scripts/windows/vscode.ps1`, `scripts/macos/vscode.sh` | есть | нет |
| Cursor (опционально) | Альтернативный AI-редактор для тех, кто к нему привык. `default: false`. | `components.json:50-63`, `scripts/windows/cursor.ps1:83-92` | есть | нет |
| Claude Code CLI | Сам ИИ-агент — команда `claude`, которой пишешь задачу словами. Офлайн из `vendor/npm-cache`, иначе установочный скрипт с сайта. | `components.json:64-80`, `scripts/windows/claude.ps1:184-247` | есть | нет |
| Расширение Claude Code для Cursor | Кнопка Claude внутри Cursor. Оба VS Code-расширения ставит `vscode.ps1` — здесь только Cursor, чтобы не было второго elevated-пути. | `components.json:81-96`, `scripts/windows/extension.ps1:34-48` | есть | нет |
| Скрытый шаг «Проверка установки» | В конце сам проверяет git/node/claude/конфиг/расширение и рисует галочки на финале. Диагностика никогда не роняет установку (всегда `exit 0`). | `components.json:97-106`, `scripts/windows/verify.ps1:43-241`, `src/renderer/app.js:1480-1483` | есть | нет |
| Конфиг Жемала (`~/.claude`) | Раскладывает готовую настройку Claude — правила, агентов, команды, скиллы. Два режима: дополнить недостающее или переустановить начисто. `~/.claude` никогда не переносится и не стирается. | `components.json:114-130`, `scripts/windows/config.ps1:33-48`, `src/install-mode.js` | есть | нет |
| Наборы скиллов (packs) с прунингом | Тематические пачки навыков вместо «всё подряд». Проверено разбором `packs.json`: **core 23 скилла + 11 паков / 186 скиллов**. Скилл вне всех паков ставится всегда. Прунинг fail-closed. | `packs.json`, `src/renderer/app.js:1213-1257`, `scripts/windows/config.ps1:292-337` | есть | нет |
| Python-пакеты (pydeps) + Playwright-браузеры | Python и библиотеки для продвинутых навыков — картинки, документы, автоматизация. Офлайн из `vendor/pywheels`, при неудаче — с PyPI. | `components.json:132-150`, `scripts/windows/pydeps.ps1:144-186` | есть | нет |
| Nomad — приватный AI-агент | Личный автономный агент с командой `nmd`: свой ключ к любой нейросети или облако без VPN. Только из вшитого `vendor/nomad-src` — клонирования нет вовсе. | `components.json:180-193`, `scripts/windows/nomad.ps1:69-83,240`, `config.json:5-17` | есть | нет |
| Поле ключа Nomad на финише | Даёт вставить ключ из кабинета уже после установки — раньше вставить было некуда. Пишет управляемый блок в `HERMES_HOME/config.yaml`. | `src/main.js:1940-1996`, `src/preload.js:37`, `src/renderer/app.js:2313-2320` | есть | нет |
| uv (только вшитый) | Быстрый менеджер Python для продвинутых навыков, полностью офлайн. Единственный компонент, который в lite-издании не докачивается. | `components.json:201-218`, `scripts/windows/uv.ps1:1-13` | есть | нет |
| AI-мост (split-tunnel к ИИ) | Зарубежный адрес только ИИ-доменам через SSH, остальной интернет напрямую. Автозапуск прописывается только после успешного self-test. | `components.json:226-238`, `agent/bridge_agent.py`, `scripts/windows/bridge.ps1:130-169` | есть | нет |
| Скрепка (маскот) | Помощник поверх окон: показывает, что делает Claude, и даёт кнопки «Разрешить/Отклонить» прямо на экране. Правит `~/.claude/settings.json` атомарно, с бэкапом. | `components.json:245-259`, `scripts/windows/mascot.ps1:8,18,55-88` | есть | нет |
| Claude Desktop (онлайн) | Нативное приложение Claude отдельным окном. Скачивается в Admins-only кэш, подпись проверяется до запуска. | `components.json:267-281`, `scripts/windows/claude-desktop.ps1:1-12` | есть | нет |
| ChatGPT Desktop (онлайн) | ChatGPT отдельным окном: на Windows из Microsoft Store, на macOS с сайта OpenAI (точный Team ID + bundle id). | `components.json:283-297`, `scripts/windows/chatgpt-desktop.ps1:1-12` | есть | нет |
| Handy — голосовой ввод | Диктовка вместо печати: зажал Ctrl+Alt+Space, продиктовал — текст вставился, распознавание локальное. Опция микрофона пишет согласие в реестр только при явном выборе. | `components.json:299-328`, `scripts/windows/handy.ps1:57-61,122` | есть | нет |
| Стартовый проект `~/HamidunStart` | Готовая папка с пятью примерами промптов, чтобы первый шаг после установки был очевиден. Дополняется, а не пропускается, если папка уже есть. | `assets/starter-project/`, `scripts/windows/config.ps1:337-386`, `package.json:93-96` | есть | нет |
| Детекция уже установленного | Перед установкой честно показывает, что на машине уже стоит, и не предлагает ставить поверх рабочего. Если системный PATH не прочитался — честный баннер вместо ложного «не установлено». | `src/main.js:2540-2784`, `src/install-manifest.js` | есть | нет |
| Удаление компонента с квитанциями | Кнопка «Удалить» сносит только то, что поставил этот установщик. Удаляемых компонентов четыре: `course`, `uv`, `mascot`, `bridge`. | `src/main.js:3236-3356`, `src/uninstall-targets.js`, `src/install-receipts.js`, `src/renderer/app.js:43` | есть | нет |
| «Переустановить начисто» (repair) | Форсирует чистую переустановку конфига, но только после явного диалога-подтверждения — нужны оба флага сразу. | `src/renderer/app.js:462-513`, `src/install-mode.js`, `src/main.js:1164-1176` | есть | нет |

---

## Целостность и гейты (7; покрыто 7)

Слой, из-за которого установщику можно доверять запуск чужих бинарей с правами администратора.

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Гейт целостности вшитых артефактов | Ни один вшитый установщик не запускается, если байты не совпали с манифестом. При любом несовпадении или отсутствии манифеста установка останавливается. | `scripts/windows/_verify.ps1`, `vendor/checksums.json`, `scripts/macos/_lib.sh`, `tools/regen-checksums.js` | есть | нет |
| Второй гейт — внутри докачанного архива | Проверяет не только архив целиком, но и конкретные исполняемые файлы внутри: `gatedFiles` есть у 19 из 23 записей реестра. Не удалось положить манифест в staging — компонент не пускается. | `remote-components.json`, `src/main.js:1064-1077`, `scripts/windows/_verify.ps1` | есть | нет |
| Де-элевация | То, что лежит в папке пользователя, никогда не исполняется с правами администратора. Единый примитив, отдельный CI-гейт на него. | `scripts/windows/_deelev.ps1`, `src/main.js:1605-1877`, `scripts/macos/_lib.sh` | есть | нет |
| Строгий allowlist окружения | Renderer не может подсунуть подменённый PATH или чужой репозиторий конфига: явный список из 13 ключей, PATH только из admin-owned каталогов, адрес репозитория — из вшитого `config.json`. | `src/install-env.js:20-28`, `src/main.js:812-862,1177-1187` | есть | нет |
| Блокирующий стоп-экран при оторванном vendor | Не даёт запустить заведомо сломанную установку, когда офлайн-файлы рядом с приложением недоступны. Проверка идёт по изданию, а не по одному файлу. | `src/main.js:32-152,890-895`, `src/renderer/app.js:810-832` | есть | нет |
| Проба сервера докачки (preflight lite) | До нажатия «Установить» проверяет, отвечает ли сервер, и предупреждает жёлтым баннером с кнопкой «Проверить снова». Сеть трогает только main — CSP renderer'а запрещает fetch. | `src/main.js:676-694`, `src/renderer/app.js:161-201` | есть | нет |
| Один экземпляр + уборка за собой | Второй запуск не плодит вторую установку; закрытие окна не оставляет висящих инсталляторов и гигабайтов мусора. Замок живёт в main, переживает Ctrl+R. | `src/main.js:196-242,298-340,348-385` | есть | нет |

---

## Экраны и сценарий ученика (18; покрыто 18)

Самая слабая по покрытию группа: ровно половина экранов и подсказок ничем не застрахована от регресса.

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Экран приветствия | Объясняет новичку, что будет поставлено, сколько займёт и куда бежать за помощью. | `src/renderer/index.html:18-87`, `src/renderer/app.js:298-324` | есть | нет |
| Экран выбора компонентов с зависимостями | Карточки компонентов, автоматическое включение зависимостей, платформенный гейт (чужая ОС — компонент не показывается вовсе и отбивается на запуске). | `src/renderer/index.html:90-142`, `src/renderer/app.js:365-542`, `src/renderer/deps.js:8-21` | есть | нет |
| Попап «что попадёт / что скачается» | Снимает страх «а не вирус ли это»: список всего с честными размерами. Где размера нет — прочерк, а не выдуманное число. | `src/renderer/app.js:868-918`, `tools/sync-sizes.js`, `remote-components.json` | есть | нет |
| Экран прогресса | Шаги, живой лог, проценты, часы на идущем шаге. Лог — кольцо из 800 строк с одной перерисовкой на кадр. | `src/renderer/index.html:145-192`, `src/renderer/app.js:1275-1416` | есть | нет |
| Прогресс докачки в мегабайтах | «Скачиваю N% · X из Y МБ», чтобы не казалось, что всё зависло. Дедлайн считается от размера архива, но не меньше 20 минут. | `src/main.js:1033`, `src/preload.js:19-23`, `src/renderer/app.js:1716-1735` | есть | нет |
| Карусель подсказок | Пока идёт долгая установка, крутятся короткие советы — раз в 12 секунд. | `src/renderer/app.js:1757-1795`, `src/renderer/index.html:178-185` | есть | нет |
| Маскот Омлетон в интерфейсе | Персонаж меняет выражение по ходу: смотрит, думает, работает, радуется. Четыре `.webp` на диске. | `src/renderer/mascot/*.webp`, `src/renderer/app.js:282-293,1905-1908` | есть | нет |
| Финишный экран «три шага» | Ровно три следующих действия и большая кнопка «Открыть VS Code». Если упал редактор — кнопка гасится, первый шаг меняется. | `src/renderer/app.js:1971-2270` | есть | нет |
| CTA бота с deep-link по результату | Ведёт застрявшего в Telegram-бота, ссылка сама сообщает боту, что упало. Хэндл только из `config.json`, хардкода нет. | `src/renderer/finish-link.js:14-47`, `src/renderer/app.js:2041-2057`, `config.json:60` | есть | нет |
| Памятка «Что дальше» офлайн | Кладёт на рабочий стол постоянную инструкцию и показывает её в окне установщика, не выдёргивая браузер. Путь только из вшитого конфига. | `assets/START-HERE.html`, `src/main.js:1526-1562`, `src/renderer/app.js:920-1010` | есть | нет |
| Мини-визард API-ключей | Даёт вписать ключи Google/OpenAI/ElevenLabs на финише. Fail-closed на чтении: любая ошибка кроме «файла нет» — ключи не сохраняются. | `src/main.js:2253-2303`, `src/renderer/app.js:2414-2434` | есть | нет |
| Кнопки запуска после установки | Открывает VS Code на папке проекта, Cursor, папку курса или терминал с `claude`. Терминал не запускается напрямую из elevated-процесса. | `src/main.js:1583-1600,1938,2003-2029,2180-2248` | есть | нет |
| Предупреждение «запущен под другой учёткой» | Ловит случай, когда UAC-пароль дал другой пользователь и всё встало бы в чужой профиль. Отдельный async-IPC, чтобы не морозить стартовый экран. | `src/main.js:439-511,564-566`, `src/renderer/app.js:109-118` | есть | нет |
| Предполётные проверки машины | Свободное место и версия ОС до старта. Best-effort: не смог замерить — просто не показывает цифру. | `src/main.js:513-559` | есть | нет |
| Каскад «пропущено из-за зависимости» | Когда падает один компонент и за ним встают пять — объясняет одной фразой, что чинить первым. Различает целостность / сеть / окружение. | `src/renderer/app.js:1418-1444,1459-1473` | есть | нет |
| Сторож молчащего шага | Если шаг десять минут не выдаёт ни строчки — честно пишет, что установка идёт и закрывать окно рано. Процесс не убивает. | `src/main.js:1288-1308` | есть | нет |
| Человеческий перевод провала | Вместо простыни из cargo/pip/npm говорит словами, что случилось и виноват ли человек. Девять видов причин, выжимка строится всегда. | `src/failure-explain.js:23-144`, `src/main.js:1376-1409` | есть | нет |
| macOS: самолечение карантина и транслокации | Кнопкой чинит «повреждённый» установщик: сам находит образ, снимает карантин, проверяет факт и перезапускается — без команд в Терминале. | `src/main.js:3358-3540`, `src/renderer/app.js:719-808` | есть | нет |

---

## Публикация и издания (8; покрыто 8)

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Два издания: офлайн и лёгкое | Один продукт в двух видах: тяжёлый файл без интернета и лёгкий, докачивающий выбранное. В lite компоненты выводятся в remote автоматически по реестру. Замер на 2026-08-25: офлайн-exe 1 438 688 139 Б, lite 87 950 026 Б. | `config.json:2`, `tools/build-lite.js`, `src/main.js:89-101,628-651` | есть | нет |
| Докачка тяжёлых компонентов из CDN | Лёгкий установщик тянет только выбранное — с двух зеркал, с обязательным sha256, admin-owned staging, только HTTPS, анти-SSRF, пиннинг DNS. Реестр: 23 записи (12 win32, 11 darwin). | `src/remote-fetch.js:23-70`, `remote-components.json`, `src/main.js:950-1078` | есть | нет |
| Предполётные гейты сборки | Не даёт выпустить установщик со вчерашним конфигом или размером, на котором makensis молча ломается. Хук `beforePack` обойти через `npx electron-builder` нельзя. | `tools/preflight-build.js`, `tools/before-pack.js`, `tools/assert-clean.js`, `tools/fetch-config.js` | есть | нет |
| Обслуживание vendor | Держит цифры на карточках правдивыми, а вес сборки под потолком: пересчёт размеров, регенерация хешей, чистка npm-кеша. `npm run sizes:check` — гейт расхождения без перезаписи. | `tools/sync-sizes.js`, `tools/regen-checksums.js`, `tools/prune-npm-cache.js` | есть | нет |
| Единый предрелизный вердикт GO/NO-GO | Один прогон вместо пяти проверок «по памяти»: свежесть vendor, сверка компонентов, реестр против checksums, живость зеркал. WARN не роняет, но печатается отдельно. | `tools/release-check.js`, `package.json:16` | есть | нет |
| Публикация артефактов в S3 | Выкладывает установщики и компоненты content-addressed, чтобы перезалив не ломал старые сборки. Fail-closed по диапазону размера при выкладке дистрибутивов. | `tools/publish-dist.py`, `tools/publish-vendor.py`, `tools/push-component.py` | есть | нет |
| CI: обязательный юнит-гейт | Каждый коммит в main проверяется автоматически, `continue-on-error` не ставится нигде. Тяжёлые прогоны (E2E, mac-сборки) — вручную перед релизом. | `.github/workflows/unit-tests.yml`, `e2e-win.yml`, `build-mac.yml`, `staging-primitive.yml` | есть | нет |
| E2E-прогон настоящего GUI | Проверяет установщик так, как его увидит человек: клики по реальному окну, реальная докачка, изолированный профиль; сценарий `preserve` сверяет каждый пользовательский файл побайтово по sha256. | `test/e2e-gui.js`, `.github/workflows/e2e-win.yml` | есть | нет |

---

## Курс (2; покрыто 1)

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Курс-симулятор + ярлык на столе | Интерактивный курс: открываешь папку в Claude Code, агент-наставник ведёт по миссиям офлайн. На macOS распаковка через `ditto` — `unzip` ломает кириллицу в именах. | `components.json:158-172`, `scripts/windows/course.ps1:29-57`, `vendor/course/vibecoding-course.zip` | есть | нет |
| Маяк завершения курса | Сигналит академии, что человек дошёл до конца. | `config.json:38-45`, `src/main.js:1132-1151`, `scripts/windows/course.ps1:46-50` | **нет** | нет |

---

## Сопутствующее (4; покрыто 4)

| Фича | Что делает | Где код | Тест | Спека |
|---|---|---|---|---|
| Телеметрия с opt-out и привязкой к человеку | Отправляет в академию, что и как встало, чтобы бот помогал адресно. Закрытый список событий, чистка ПД, uid ставит только главный процесс. Галочку можно снять. | `src/main.js:1444-1496`, `src/uid-telemetry.js`, `src/renderer/app.js:1825-1966`, `config.json:46-49` | есть | нет |
| Журнал установки на диске | Пишет всё в один файл `~/.hamidun-setup/install.log`, который человек может целиком отправить в поддержку. Один удерживаемый дескриптор вместо записи на каждой строке. | `src/main.js:169-191,1935` | есть | нет |
| Аудит работоспособности конфига | Проверяет не «файлы разложились», а что конфиг на машине ученика реально работает: включённые плагины без маркетплейса, объявленные но не поднявшиеся MCP, правила со ссылками на несуществующие скрипты. | `tools/audit-pack.js`, `test/audit-pack-regex.test.js`, `test/audit-pack-run.test.js` | есть | нет |
| Режим холостого прогона (dry-run) | Показывает, что именно будет сделано, ничего не трогая: без докачки, без записи журнала, деинсталляция печатает WOULD/KEEP. | `src/main.js:930-935,980-985,3291-3295`, все `scripts/*/*.ps1\|sh`, `test/clean-machine-test.ps1` | есть | нет |
